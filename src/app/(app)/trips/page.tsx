import { PagePlaceholder } from "@/components/app/page-placeholder";

export default function TripsPage() {
  return (
    <PagePlaceholder
      title="Trips"
      description="Trip dispatcher with live validation and lifecycle actions."
    />
  );
}
