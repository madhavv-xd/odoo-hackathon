import { PagePlaceholder } from "@/components/app/page-placeholder";

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Expenses"
      description="Fuel logs, expenses and per-vehicle operational cost."
    />
  );
}
